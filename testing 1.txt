#include <iostream>
#include <string>
#include <vector>

namespace A
{
    int size;
    int i;
    int e;
    int combination;
    
    int Option()
    {
        std::cout << "Please type the number of elements that you want to find the combination : ";                 
        std::cin >> size; // user input number of elements
        std::cout << "You want to find the combination from " << size << " elements\n" // shows user input
                  << "Please type the elements you want\n"; // user starts to input the elements
        return size;
    }
    
    int CombinationCal()
    {
        for (i = 0; i < size; i++)
        {
            std::cin >> e;
            combination[] = e;
        }
    
        return combination;
    }
    
        
}

namespace B
{
    int Option()
    {
        int size;
        std::cout << "Please type the number of elements that you want to find the combination : ";                 
        std::cin >> size;
        std::cout << "You want to find the combination from " << size << " elements\n"
                  << "Please type the elements you want\n";
        return size;
    }   
   
}

int main()
{
    A::Option();
    A::CombinationCal();
    std::cout << A::size;
    for (A::i = 0; A::i < A::size ; A::i++)
    {
        std::cout << A::combination[A::i];
    }

}








